import { store as auth } from '@/modules/auth'
import { store as cargo } from '@/views/cargo'
import { store as cliente } from '@/views/clientes'
import { store as usuario } from '@/views/usuario'
import { store as tipoconta } from '@/views/tipoconta'

export default {
  auth,
  cargo,
  cliente,
  usuario,
  tipoconta
}
